# moekoch.xyz — Next.js

Rebuilt from the original static HTML/Three.js site into Next.js (App Router) with the same structure, styling, and interactive 3D flower hero.

## Run locally
```bash
npm install
npm run dev
```

## Notes
- Add `public/resume.pdf` and `public/portrait.jpg` (falls back to a placeholder image if missing).
- Project modal content, timeline, capabilities, and lab sections are ported 1:1 from the original.
